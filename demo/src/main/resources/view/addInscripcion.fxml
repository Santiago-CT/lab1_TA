<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<ScrollPane fitToWidth="true" prefHeight="650.0" prefWidth="520.0"
            xmlns="http://javafx.com/javafx/17.0.7"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.controllerFXML.AddInscripcionController"
            stylesheets="@../styles/profesor.css"
            styleClass="modal-container">

    <VBox spacing="20.0" prefWidth="500.0">

        <padding>
            <Insets bottom="30.0" left="30.0" right="30.0" top="30.0"/>
        </padding>

        <!-- Título -->
        <HBox alignment="CENTER_LEFT" spacing="10.0">
            <Label styleClass="icon-large" text="👨‍🎓">
                <font>
                    <Font size="28.0"/>
                </font>
            </Label>
            <Label styleClass="title-large" text="Agregar">
                <font>
                    <Font name="System Bold" size="24.0"/>
                </font>
            </Label>
        </HBox>

        <Separator styleClass="form-separator"/>

        <!-- Formulario -->
        <VBox spacing="15.0" styleClass="form-container">
            <!-- Curso -->
            <VBox spacing="5.0">
                <Label styleClass="form-label" text="Curso *">
                    <font>
                        <Font name="System Bold" size="13.0"/>
                    </font>
                </Label>
                <ComboBox fx:id="cmbCurso" prefHeight="35.0" prefWidth="460.0" promptText="Seleccione el curso"
                          styleClass="form-field"/>
            </VBox>

            <!-- Estudiante -->
            <VBox spacing="5.0">
                <Label styleClass="form-label" text="Estudiante *">
                    <font>
                        <Font name="System Bold" size="13.0"/>
                    </font>
                </Label>
                <ComboBox fx:id="cmbEstudiante" prefHeight="35.0" prefWidth="460.0" promptText="Seleccione el estudiante"
                          styleClass="form-field"/>
            </VBox>

            <!-- Año -->
            <VBox spacing="5.0">
                <Label styleClass="form-label" text="Año *">
                    <font>
                        <Font name="System Bold" size="13.0"/>
                    </font>
                </Label>
                <TextField fx:id="txtAnio" prefHeight="35.0" promptText="Digite el año"
                           styleClass="form-field"/>
            </VBox>

            <!-- Estudiante -->
            <VBox spacing="5.0">
                <Label styleClass="form-label" text="Semestre *">
                    <font>
                        <Font name="System Bold" size="13.0"/>
                    </font>
                </Label>
                <ComboBox fx:id="cmbSemestre" prefHeight="35.0" prefWidth="460.0" promptText="Seleccione el semestre"
                          styleClass="form-field"/>
            </VBox>
            <!-- Mensaje de campos requeridos -->
            <Label styleClass="help-text" text="* Campos requeridos">
                <font>
                    <Font size="11.0"/>
                </font>
            </Label>
        </VBox>

        <!-- Separador -->
        <Separator styleClass="form-separator"/>

        <!-- Botones de acción -->
        <HBox alignment="CENTER_RIGHT" spacing="15.0">
            <Button fx:id="btnCancelar" onAction="#cancelar" prefHeight="40.0" prefWidth="120.0"
                    styleClass="modern-button, button-secondary" text="❌ Cancelar"/>
            <Button fx:id="btnGuardar" onAction="#save" prefHeight="40.0" prefWidth="120.0"
                    styleClass="modern-button, button-success" text="💾 Guardar"/>
        </HBox>

        <!-- Mensaje de estado -->
        <Label fx:id="lblMensaje" styleClass="message-label" wrapText="true"/>

    </VBox>
</ScrollPane>